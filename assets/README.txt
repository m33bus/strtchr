This version is built around the specific uploaded head.glb.

Included already:
- assets/bg.jpg
- assets/head.glb

You only need to add:
- assets/audio/sound1.wav
- assets/audio/sound2.wav
- assets/audio/sound3.wav
- assets/audio/sound4.wav

Notes:
- The uploaded head.glb contains one mesh, vertex colors, and no baked normals.
- This scaffold computes normals at runtime and frames the camera specifically around this model.
- Audio is optional at load time and never blocks the page.
